import React from "react";

const UserProfile = () => {
	// Simulation d'une erreur
	// const simulateError = undefined;
	// console.log(simulateError.someProperty); // Ceci provoquera une erreur

	return (
		<div>
			<h2>User Profile</h2>
			<p>This is the user profile component.</p>
		</div>
	);
};

export default UserProfile;
